<table>
    <thead>
    <th>Icon</th>
    <th>Name</th>
    <th>Created</th>
    <th>Updated</th>
    <th>Actions</th>
    </thead>
    <tbody>
    {% for category in categories %}
        <tr>
            <td>{{ category.icon }}</td>
            <td>{{ category }}</td>
            <td>{{ category.createdAt|date('d.m.Y H:i:s') }}</td>
            <td>{{ category.updatedAt|date('d.m.Y H:i:s') }}</td>
            <td>
                <a href="{{ path('category_edit', {'id': category.id}) }}">Edit</a>
                <a href="#"
                   class="js-category-delete"
                   data-url="{{ path('category_delete', {'id': category.id}) }}"
                   data-name="{{ category.name }}">Delete</a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
